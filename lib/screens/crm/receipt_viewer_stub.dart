import 'package:flutter/material.dart';

/// Non-web stub: returns null to signal the caller should use the
/// mobile fallback rendering path (printing.PdfPreview etc.).
Widget? buildBrowserNativePdfViewer(String url) => null;
